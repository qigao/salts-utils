add_subdirectory(cxml)
add_subdirectory(monocypher)
if(TARGET cxml AND ANDROID)
  target_compile_options(cxml PRIVATE -Wno-error)
endif()
if(TARGET cxml_tests)
  if(NOT BUILD_TESTING)
    set_target_properties(cxml_tests PROPERTIES EXCLUDE_FROM_ALL TRUE)
  endif()
  if(ANDROID)
    target_compile_options(cxml_tests PRIVATE -Wno-error)
  endif()
endif()
