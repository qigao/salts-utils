[
  {
    "name": "cycle_evaluates_nested_collection_arguments_before_selection",
    "template": "{% for item in [1] %}{{ loop.cycle([loop.changed(item)], [loop.changed(item + 1)]) }}:{{ loop.changed(item + 1) }}{% endfor %}",
    "context": {},
    "expected": {
      "status": "ok",
      "output": "[True]:False"
    }
  }
]
