schema ServiceNative [version(7)];

message AddRequest {
    uint32 left;
    optional uint32 scale default 1;
}

message AddResponse {
    optional uint32 sum;
}

service Calc {
    Add: AddRequest -> AddResponse;
}
